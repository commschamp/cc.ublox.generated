// Generated by commsdsl2comms v7.0.0

/// @file
/// @brief Contains definition of <b>"MGA-FLASH-DATA"</b> message and its fields.

#pragma once

#include <algorithm>
#include <cstdint>
#include <limits>
#include <tuple>
#include "cc_ublox/MsgId.h"
#include "cc_ublox/field/FieldBase.h"
#include "cc_ublox/message/MgaFlashEphCommon.h"
#include "cc_ublox/options/DefaultOptions.h"
#include "comms/MessageBase.h"
#include "comms/field/ArrayList.h"
#include "comms/field/IntValue.h"
#include "comms/options.h"

namespace cc_ublox
{

namespace message
{

/// @brief Fields of @ref MgaFlashEph.
/// @tparam TOpt Extra options
/// @see @ref MgaFlashEph
/// @headerfile cc_ublox/message/MgaFlashEph.h
template <typename TOpt = cc_ublox::options::DefaultOptions>
struct MgaFlashEphFields
{
    /// @brief Definition of <b>"type"</b> field.
    class Type : public
        comms::field::IntValue<
            cc_ublox::field::FieldBase<>,
            std::uint8_t,
            comms::option::def::HasName,
            comms::option::def::FailOnInvalid<>,
            comms::option::def::FixedValue,
            comms::option::def::DefaultNumValue<1>,
            comms::option::def::ValidNumValue<1>
        >
    {
        using Base =
            comms::field::IntValue<
                cc_ublox::field::FieldBase<>,
                std::uint8_t,
                comms::option::def::HasName,
                comms::option::def::FailOnInvalid<>,
                comms::option::def::FixedValue,
                comms::option::def::DefaultNumValue<1>,
                comms::option::def::ValidNumValue<1>
            >;
    public:
        /// @brief Re-definition of the value type.
        using ValueType = typename Base::ValueType;

        /// @brief Compile time detection of special values presence.
        static constexpr bool hasSpecials()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::TypeCommon::hasSpecials();
        }

        /// @brief Name of the field.
        static const char* name()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::TypeCommon::name();
        }
    };

    /// @brief Definition of <b>"version"</b> field.
    class Version : public
        comms::field::IntValue<
            cc_ublox::field::FieldBase<>,
            std::uint8_t,
            comms::option::def::HasName,
            comms::option::def::ValidNumValue<0>
        >
    {
        using Base =
            comms::field::IntValue<
                cc_ublox::field::FieldBase<>,
                std::uint8_t,
                comms::option::def::HasName,
                comms::option::def::ValidNumValue<0>
            >;
    public:
        /// @brief Re-definition of the value type.
        using ValueType = typename Base::ValueType;

        /// @brief Compile time detection of special values presence.
        static constexpr bool hasSpecials()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::VersionCommon::hasSpecials();
        }

        /// @brief Name of the field.
        static const char* name()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::VersionCommon::name();
        }
    };

    /// @brief Definition of <b>"sequence"</b> field.
    class Sequence : public
        comms::field::IntValue<
            cc_ublox::field::FieldBase<>,
            std::uint16_t,
            comms::option::def::HasName
        >
    {
        using Base =
            comms::field::IntValue<
                cc_ublox::field::FieldBase<>,
                std::uint16_t,
                comms::option::def::HasName
            >;
    public:
        /// @brief Re-definition of the value type.
        using ValueType = typename Base::ValueType;

        /// @brief Compile time detection of special values presence.
        static constexpr bool hasSpecials()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::SequenceCommon::hasSpecials();
        }

        /// @brief Name of the field.
        static const char* name()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::SequenceCommon::name();
        }
    };

    /// @brief Definition of <b>"size"</b> field.
    class Size : public
        comms::field::IntValue<
            cc_ublox::field::FieldBase<>,
            std::uint16_t,
            comms::option::def::HasName
        >
    {
        using Base =
            comms::field::IntValue<
                cc_ublox::field::FieldBase<>,
                std::uint16_t,
                comms::option::def::HasName
            >;
    public:
        /// @brief Re-definition of the value type.
        using ValueType = typename Base::ValueType;

        /// @brief Compile time detection of special values presence.
        static constexpr bool hasSpecials()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::SizeCommon::hasSpecials();
        }

        /// @brief Name of the field.
        static const char* name()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::SizeCommon::name();
        }
    };

    /// @brief Definition of <b>"data"</b> field.
    class Data : public
        comms::field::ArrayList<
            cc_ublox::field::FieldBase<>,
            std::uint8_t,
            typename TOpt::message::MgaFlashEphFields::Data,
            comms::option::def::HasName,
            comms::option::def::SequenceLengthForcingEnabled
        >
    {
        using Base =
            comms::field::ArrayList<
                cc_ublox::field::FieldBase<>,
                std::uint8_t,
                typename TOpt::message::MgaFlashEphFields::Data,
                comms::option::def::HasName,
                comms::option::def::SequenceLengthForcingEnabled
            >;
    public:
        /// @brief Name of the field.
        static const char* name()
        {
            return cc_ublox::message::MgaFlashEphFieldsCommon::DataCommon::name();
        }
    };

    /// @brief All the fields bundled in std::tuple.
    using All = std::tuple<
        Type,
        Version,
        Sequence,
        Size,
        Data
    >;
};

/// @brief Definition of <b>"MGA-FLASH-DATA"</b> message class.
/// @details
///     See @ref MgaFlashEphFields for definition of the fields this message contains.
/// @tparam TMsgBase Base (interface) class.
/// @tparam TOpt Extra options
/// @headerfile cc_ublox/message/MgaFlashEph.h
template <typename TMsgBase, typename TOpt = cc_ublox::options::DefaultOptions>
class MgaFlashEph : public
    comms::MessageBase<
        TMsgBase,
        typename TOpt::message::MgaFlashEph,
        comms::option::def::StaticNumIdImpl<cc_ublox::MsgId_MgaFlash>,
        comms::option::def::FieldsImpl<typename MgaFlashEphFields<TOpt>::All>,
        comms::option::def::MsgType<MgaFlashEph<TMsgBase, TOpt> >,
        comms::option::def::HasName,
        comms::option::def::HasCustomRefresh
    >
{
    // Redefinition of the base class type
    using Base =
        comms::MessageBase<
            TMsgBase,
            typename TOpt::message::MgaFlashEph,
            comms::option::def::StaticNumIdImpl<cc_ublox::MsgId_MgaFlash>,
            comms::option::def::FieldsImpl<typename MgaFlashEphFields<TOpt>::All>,
            comms::option::def::MsgType<MgaFlashEph<TMsgBase, TOpt> >,
            comms::option::def::HasName,
            comms::option::def::HasCustomRefresh
        >;

public:
    /// @brief Provide names and allow access to internal fields.
    /// @details See definition of @b COMMS_MSG_FIELDS_NAMES macro
    ///     related to @b comms::MessageBase class from COMMS library
    ///     for details.
    ///
    ///     The generated values, types and functions are:
    ///     @li @b FieldIdx_type index, @b Field_type type and @b field_type() access fuction
    ///         for @ref MgaFlashEphFields::Type field.
    ///     @li @b FieldIdx_version index, @b Field_version type and @b field_version() access fuction
    ///         for @ref MgaFlashEphFields::Version field.
    ///     @li @b FieldIdx_sequence index, @b Field_sequence type and @b field_sequence() access fuction
    ///         for @ref MgaFlashEphFields::Sequence field.
    ///     @li @b FieldIdx_size index, @b Field_size type and @b field_size() access fuction
    ///         for @ref MgaFlashEphFields::Size field.
    ///     @li @b FieldIdx_data index, @b Field_data type and @b field_data() access fuction
    ///         for @ref MgaFlashEphFields::Data field.
    COMMS_MSG_FIELDS_NAMES(
        type,
        version,
        sequence,
        size,
        data
    );

    // Compile time check for serialisation length.
    static const std::size_t MsgMinLen = Base::doMinLength();
    static_assert(MsgMinLen == 6U, "Unexpected min serialisation length");

    /// @brief Name of the message.
    static const char* doName()
    {
        return cc_ublox::message::MgaFlashEphCommon::name();
    }

    /// @brief Generated read functionality.
    template <typename TIter>
    comms::ErrorStatus doRead(TIter& iter, std::size_t len)
    {
        auto es = comms::ErrorStatus::Success;
        do {
            es = Base::template doReadUntilAndUpdateLen<FieldIdx_data>(iter, len);
            if (es != comms::ErrorStatus::Success) {
                break;
            }

            readPrepare_data();

            es = Base::template doReadFrom<FieldIdx_data>(iter, len);
        } while (false);

        return es;
    }

    /// @brief Generated refresh functionality.
    bool doRefresh()
    {
       bool updated = Base::doRefresh();
       updated = refresh_data() || updated;
       return updated;
    }

private:
    void readPrepare_data()
    {
        field_data().forceReadLength(
            static_cast<std::size_t>(field_size().getValue()));
    }

    bool refresh_data()
    {
        auto lenValue = field_size().getValue();
        auto realLength = field_data().value().size();
        if (static_cast<std::size_t>(lenValue) == realLength) {
            return false;
        }

        using LenValueType = typename std::decay<decltype(lenValue)>::type;
        static const auto MaxLenValue = static_cast<std::size_t>(std::numeric_limits<LenValueType>::max());
        auto maxAllowedLen = std::min(MaxLenValue, realLength);
        field_size().setValue(maxAllowedLen);
        if (maxAllowedLen < realLength) {
            field_data().value().resize(maxAllowedLen);
        }
        return true;
    }
};

} // namespace message

} // namespace cc_ublox
